package postgraduate.leetcd.xunLian;

import postgraduate.leetcd.link.DetectCycle;

/**
 * leetcode ID 17
 * 给定-个仅包含数字2-9的字符串,返回所有它能表示的字母组合。答案可以按任意顺序返回。
 * 给出数字到字母的映射如下(与电话按键相同)。注意1不对应任何字母。
 * 2-9的数字对应键盘的九键，例如2对应的字母 有abc,3对应的def....
 * 示例 1:
 * 输入: digits = "23"
 * 输出: ["ad","ae","af","bd","be", "bf","cd","ce","cf"]
 * 示例 2:
 * 输入: digits = ""
 * 输出: []
 * 示例 3:
 * 输入: digits = "2"
 * 输出: ["a","b","c"]
 */
public class MakePhoneNum {
/*
    public static void main(String[] args) throws IOException {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String s = br.readLine();
        solve(s);
    }

    private static List<String> solve(String digits) {
        if (digits.length() == 0)
            return new ArrayList<String>();

        String[] num = digits.split("");
        addStr(num, new ArrayList<String>(num.length));
    }

    private static ArrayList<String> addStr(String[] num, ArrayList<String> list) {
        if (list.size() == Math.pow(3, num.length)){
            return list;
        }
        for (int i = 0; i < list.size(); i++) {
            list.set(i,)
        }
    }
*/


}

